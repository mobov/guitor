import { getVueInstance } from './utils'

interface showOpts {
  flowId: string
  target: string
  transition?: string
}

// export default class show {
//   flowId = ''
//   target = 'root'
//   transition = 'none'
//   targetIns = null
//   constructor (opts = {} as showOpts) {
//     this.target = opts.target
//     this.flowId = opts.flowId
//   }
//   public run (vm) {
//     return new Promise((resolve, reject) => {
//
//     })
//   }
// }

export default function (vm: any, opts) {
  // const dom = getVueInstance()
  // const trigger = getVueInstance(opts.target)
  // const target = getVueInstance(opts.target)
  // if (target) {
  //
  // }
}
